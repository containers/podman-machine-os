---
# See the documentation for more information:
# https://packit.dev/docs/configuration/

jobs:
  # All tests specified in the `/plans/` subdir
  - job: tests
    trigger: pull_request
    skip_build: true
    notifications:
      failure_comment:
        message: "test failed. @containers/packit-build please check."
    targets:
      - fedora-rawhide
    identifier: machine_os_build_test
